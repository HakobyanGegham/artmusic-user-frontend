import { ApplicationProgram } from './application-program';

describe('ApplicationProgram', () => {
  it('should create an instance', () => {
    expect(new ApplicationProgram()).toBeTruthy();
  });
});
