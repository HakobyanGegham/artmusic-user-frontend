import { UploadPercentageDirective } from './upload-percentage.directive';

describe('UploadPercentageDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadPercentageDirective();
    expect(directive).toBeTruthy();
  });
});
